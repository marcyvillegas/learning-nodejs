

/* USER DEFINED MODULE */

const pi = 3.1416;

let add = (a,b) => a + b;
let subtarct = (a,b) => a - b;
let multiply = (a,b) => a * b;
let divide = (a,b) => a / b;

module.exports = {
    pi: pi,
    add: add,
    subtarct: subtarct,
    multiply: multiply,
    divide: divide
}

